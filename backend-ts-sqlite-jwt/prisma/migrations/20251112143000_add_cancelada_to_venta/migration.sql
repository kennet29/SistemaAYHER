-- Restored placeholder migration to satisfy Prisma (no-op)
PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
-- No changes: column `cancelada` already exists in DB and schema
COMMIT;
PRAGMA foreign_keys=ON;

