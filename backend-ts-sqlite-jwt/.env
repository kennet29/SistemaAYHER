# JWT
JWT_SECRET=AYHER
JWT_EXPIRES_IN=1d

# Prisma/SQLite
DATABASE_URL="file:./dev.db"

# Seed (admin inicial)
ADMIN_NAME=Admin
ADMIN_EMAIL=admin@local.test
ADMIN_PASSWORD=admin123

# Monta un respaldo adicional dentro de la unidad `C:/respaldo` para que el archivo viva fuera del repo pero siga siendo accesible por el host.
# Asegúrate de crear esa carpeta (ej. `mkdir C:\respaldo`) antes de arrancar el stack.
DESKTOP_BACKUP_MOUNT=C:/respaldo
