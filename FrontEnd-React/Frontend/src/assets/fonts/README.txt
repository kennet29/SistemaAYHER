Place your local webfont files here to avoid external font requests.

Suggested files (WOFF2):
- Inter-Variable.woff2 (full variable font or a regular subset)
- Poppins-SemiBold.woff2 (optional, headings)

Already wired CSS: ../assets/fonts/fonts.css
No code changes needed after copying files; the app will use them automatically.
